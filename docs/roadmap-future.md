# Future

<!-- Deferred work. Maintained by /roadmap. Do not put Phase/Group/Track structure here. -->

## Future

- **`/roadmap` does not own the `verify:` field it regenerates** — every card carries `_session: … · verify: …`, but the card template has no such field (four "verify" hits in the skill are prose about checking claims against git). Track 37A fixed the cause (`AGENTS.md ## Testing` now names `./bin/check`) and normalized the live strings once. Teaching the skill the field, or carrying only a scope with the runner implied, is a change to a skill outside this repo. `[source:manual]` 2026-09-01.
- **No dependency lockfile** — `pyproject.toml` declares six `>=` ranges with no lockfile and no hashes. Content-hash staleness in `bin/check` answers "does this venv match declared inputs," not reproducibility. A `.conductor` setup hook makes an unpinned `pip install` automatic. Real decision, real cost; not a workspace fix. `[source:manual]` 2026-09-01.
- **Unknown-enum wire posture** — conditional on Track 36B's acceptor. If unknown `token_sources` names are tolerated, write down which other closed vocabularies (host families, reader reasons, model ids) are open vs closed, rather than leaving it to the next retirement. `[source:plan-eng-review]` 2026-09-01. **Condition resolved 2026-09-02:** 36B's acceptor (#156) DOES retain unknown names, bounded by `_TOKEN_SOURCE_ID_RE`. The open-vs-closed posture is now recorded piecemeal in `docs/invariants/events-retro.md` (`token_sources` open, `_host_skip_phrase` closed, `[skills] agents` open) but not as one catalog — which is what keeps this deferred rather than discharged.

- **cli.py micro-cleanups** — retain only the unresolved `_resolve_mm_events_dir` and status-enum follow-up; Track 18A owns the import and `_empty_outcomes` portions.
- **`_resolve_interactive_loop` decomposition** — collector removal fired its old trigger, but the 630-line function still needs a dedicated discovery/design pass before code commitment.
- **Two-machine test bootstrap duplication** — consolidate the repeated setup only when adjacent test work touches both modules.
- **Cold-cache budget leftovers** — revisit identity-refresh and per-jsonl deadline concerns only with fresh measurements. The clause that read "root discovery is shipped" was falsified on 2026-08-25 (measured: the 50 ms autopush budget returns zero roots, the 100 ms interactive budget returns two, and a complete pass needs ~430 ms); Group 29 owns that half now.
- **identity.py micro-DRY and token-cache pins** — keep as low-priority hygiene.
- **v0.11.17 doc-drift cleanup** — re-evaluate only with an invariant-doc pass.
- **Incremental-resume accepted divergences** — act only on evidence from a corpus census.
- **Future-clamped peer mtime** — advisory watch item.
- **`_promote_target_will_sync` ignores `exclude_patterns`** — rare exclude-glob miss.
- **Peers never resolved against can be mtime-skipped by the drain** — watch after Group 12's shipped fix.
- **Abort transactionality** — pre-existing torn-state concern.
- **Price-cache TTL split** — wire-format work that competes with token-cache ownership.
- **Model-ID variant suffix aliases** — defer until a real variant appears in a census.
- **Rendered-cost sanity ceiling** — presentation safeguard without an observed bad value.
- **Parallel blob fetch** — revisit after user-reported sustained slow pulls.
- **Selective sync** — wait for a user with a large-project filtering need.
- **Mtime hash cache** — revisit only if push latency becomes user-visible again.
- **Three-way merge base** — wait for a divergence-misclassification report.
- **`mm rekey` passphrase rotation** — post-1.0 format-v3 migration work.
- **Blob-directory peer recovery** — wait for a real corrupt-manifest support case.
- **PyPI publish workflow** — wait for a distribution-demand signal.
- **Cross-device source rename identity** — known limitation until an incident demonstrates it.
- **Explicit upgrade check** — add only when cached status is insufficient.
- **Subprocess pipx upgrade** — revisit after nudge UX proves inadequate.
- **`MM_NO_VERSION_CHECK=1`** — add only for demonstrated CI ergonomics.
- **GitHub-tag pagination** — deferred until tag count approaches the API page limit.
- **Autouse devices-write-lock coupling** — forward-defense only.
- **`[retro].deny_emails`** — wait for a credential or account-hygiene need.
- **Snapshot-level completeness** — separate data-model design; do not conflate with host snapshots.
- **Retro-card machine/cause diagnostics** — defer until the baseline model card lands.
- **`mm diag` discoverability and analytics** — keep scoped to the existing command rather than adding a new one.
- **Diagnostic-string quality pass** — adopt only with a broader user-facing notice pass.
- **CT-4 enforcement and short-write handling** — storage-boundary hardening outside this batch.
- **Residual Track 16A coverage gaps** — three defensive or theoretical cases from the ship audit. _Source: [ship] coverage audit 2026-08-15._
- **Host-usage cache GC reaper** — Deferred until a fresh inventory demonstrates stale Codex or Grok cache accumulation that complete-pass pruning does not remove (for example, an inactive reader). Limit any reaper to current-reader cache formats, extend `mm gc` and its dry-run path together, and preserve partial-pass progress and complete-pass pruning. Remove retired OpenCode support and the already-shipped Group 32 prerequisite from this scope. _Source: unprocessed host-cache follow-up 2026-08-17; narrowed after code review at f10bf34, user-approved 2026-09-05._
- **Active host-session degradation policy** — consider skipping a stale or partial final rollout when its next completed record restates usage; preserve all-or-nothing publication until that proof exists. **Basis changes under Group 32:** once usage is read per turn, a stale or partial final rollout costs one turn rather than a whole session, which is most of the reason the all-or-nothing rule was held in place. Re-evaluate after Group 32, not before. _Source: unprocessed host-usage follow-up 2026-08-17._
- **Warm host-scan scaling** — revisit fingerprint-every-file cost with a measured corpus before the 250 ms autopush budget becomes user-visible. Group 32's per-turn read changes the cost profile this bullet asks to measure, so any measurement predating it is void. _Source: unprocessed host-cache follow-up 2026-08-17._
- **Machine-readable GC outcomes** — expose orphan-blob outcomes only when an automation or audit consumer needs them; Track 17D's reaper scope stays as shipped. _Source: unprocessed GC follow-up 2026-08-17._
- **Deterministic demo/fixture path for `retro-fleet`** — fresh-clone time-to-first-output is 10-30 min and nondeterministic (3.11+ interpreter, venv, editable install, `mm init`, an enabled host source, a substantive push, two aggregator passes). A `--demo` flag over a bundled synthetic corpus would make the card reproducible in three commands. The test suite is the current deterministic path and is adequate for CI, so this is ergonomics, not correctness. **Derived independently FOUR times** — Track 23A's DX pass, 33A's, 35A's, and the 2026-09-01 regen. Track 35A was the first Track whose output a user could not see at all without a second machine cooperating. **One clause has rotted:** the bullet argued in part from "a flag that is not in `--help` or README"; `--dump-host-usage` is documented at `README.md:193` and is a real `typer.Option` at `cli.py:6366`, so re-derive the TTHW claim before acting. A **"Fleet retro in five minutes" README section** is the cheaper answer to the same wall and was requested independently by Track 34A's DX pass. Note Track 37A now removes the venv/editable-install half of the friction. _Source: [23A] inbox 2026-08-18; re-derived [33A] 2026-08-28, [35A] 2026-09-01._
- **Machine-readable retro metrics export** — add `mm retro-fleet --format json` only when a structured consumer needs it. The removed v0.12.0 snapshot JSON was the de-facto export surface; a scheduled `mm retro-fleet 7d --format json >> ~/retro-history.jsonl` would be a better long-horizon archive. _Source: [24B] T12 / DX-6 2026-08-22._
- **Bounded binary retro-event JSONL reader** — `_iter_jsonl` (`aggregator.py:630`) uses unbounded text iteration with replacement decoding, unlike `token_usage.iter_bounded_lines` and `pullhistory._yield_lines`. Re-verified unbounded at `727f9cd`. Preserve the one-materialisation prior-period design while making malformed-input handling bounded and byte-safe. **Measured datum merged from a 2026-08-30 duplicate:** a 2.8 MB / 40,000-model row is fully decoded by `json.loads` before any cardinality cap runs, allocating 10.6 MB. This predates Track 33A and applies to EVERY field on EVERY row type (`hosts`, `projects`, `skills_by_day`) — a peer could already do it with a giant `hosts` map. Threat model is a compromised peer inside the user's own E2E-encrypted fleet, which is why it is not a release blocker. **The duplicate named `aggregator._iter_event_objects`, a symbol that has never existed in this repo** (`grep` 0 hits, `git log -S` 0 commits); it was killed and its one new fact folded in here. _Source: [24B] T12 / Eng F1 2026-08-22; datum from [33A] Codex pass 2, 2026-08-30._
- **Accept a bare integer retro window** — `mm retro-fleet 7` is rejected while `7d` works. The skill translates natural language so agents never hit it; direct CLI users do. Re-confirmed live 2026-08-25: `mm retro-fleet 7` still exits with `window must be of the form Nd`. Left deferred rather than folded into Group 34 or 35 — unrelated ergonomics, and it would push either card past the weight cap. _Source: [23A] inbox 2026-08-18._
- **Deregister/prune retired devices** — a retired-but-registered Mac inflates every "N of M machines" denominator forever, which is the root cause 23A's coverage wording worked around rather than fixed. Wants `mm devices --prune` or a staleness nudge. _Source: [23A] inbox 2026-08-18._
- **Two releases share the version string `0.11.23`** — `CHANGELOG.md` carries `## [0.11.23] - 2026-05-06` and `## [0.11.23] - 2026-05-05` as separate releases; `docs/PROGRESS.md` charts only the 2026-05-05 one, so the 2026-05-06 "auto-pin iCloud storage on `mm init`" release has no row. `test_every_changelog_version_has_a_progress_row` matches on version string, so the gate cannot see this and reports parity. Needs a decision: renumber one release, or teach the gate to key on (version, date). _Source: /ship adversarial review 2026-08-21._
- **Fleet-wide skill-link visibility** — markers and links are per-machine, so nothing answers "which of my Macs has a wedged link", which is the real question behind surfacing a wedge. One field on the mm-push event would let the retro card answer it. Deferred as a wire-format change, outside the installer's blast radius. _Source: /autoplan Phase 3 eng review 2026-08-20._
- **install-skills `--dry-run`/`--force`/`--json`** — both DX voices wanted this surface (`--force` renames to `retro-fleet.mm-backup-<ts>`, never deletes; exit 3 when `--dry-run` would change something; no `--migrate-legacy`; no `--store PATH`; `MM_SKILLS_DIR` for tests). Parked so 25B stays markdown-only and can room with the registry. 24A already makes `dry_run=True` return full classifications. _Source: /autoplan Phase 3.5 DX X7 2026-08-21._
- **Race-safe skills-dir ancestor pin (`dir_fd` + `O_NOFOLLOW`)** — Approach B makes dangling repair and the `lockedjson` ownership ledger unnecessary; this POSIX leftover from old 29A is hardening, not a wedge fix. _Source: /autoplan Phase 3 eng E1 correction 2026-08-21._
- **`SkillInstallResult.action` field** — the enum still mixes observed state with outcome (`installed` cannot distinguish created/migrated/repaired). `_failed_result` still throws `operation` away. A store-publish failure should render as one failure, not three. _Source: /autoplan Phase 3.5 DX X9 2026-08-21._

- **`mm skill-run <name> --protocol N` handshake** — give agent skills an executable boundary so the binary owns capability checks, store/link hash diagnosis, structured remediation, and fail-closed exit codes. A new skill against an old binary then fails *by construction* (unknown subcommand or flag), which is the one thing a version check written into SKILL.md can never do — Track 25B cut its version-comparison stage precisely because `min_mm_version` is written by the same binary whose freshness is in question. This is also the only route to detecting that the SKILL.md the agent loaded differs from the store, which is undetectable today and is the one critical gap 25B shipped with. Keep `mm retro-fleet` as the human/script surface; do not overload it with agent-handshake semantics. Reached independently by all three review phases of the 25B `/autoplan`. _Source: /autoplan Phase 1 CEO C-5 / Eng E-1 / DX-5, 2026-08-22._
- **Surface store-vs-binary skew in `mm status` / `mm diag`** — the binary-side half of Track 25B's Step 0, and the only version of that signal that reaches users who never upgrade: Step 0 ships *through* the store republish, which requires an `mm` at least as new as the store, so the population it protects is the population that cannot receive it. Also the only instrumentable version — a markdown preflight cannot be measured, which is why 25B's DX measurement pass scored 3/10. Note `diagnose_skill_links` already emits `store_state` and `store_version` (`skill_link.py:1400` and `:1441`, re-anchored 2026-09-01; the filed `:1289` / `:1306-1307` had rotted); this is a surfacing change, not a data one. Deferred because Group 26 owned `cli.py` that window. _Source: /autoplan Phase 1 CEO E6 / Section 8, 2026-08-22._ **Third consecutive Track has now wanted it** (34A, 35A, and the 2026-09-01 regen), but the framing is DOWNGRADED, not raised: Track 34A shipped the verbatim-never-interpreted fallback (`retro_fleet/SKILL.md:293`), so a stale installed copy degrades safely rather than fabricating. This is narration quality, not correctness — which is why it stays deferred despite the repeat count.
- **Nothing wires `mm autopush` on the maintainer fleet** — verified 2026-08-25 on device `3a6c7dc9`: no Claude Code hook, no launchd agent, no cron entry, and `~/.config/mind-meld/autopush.log` last written 2026-08-15, while `README.md` documents a CLAUDE.md snippet for exactly this. Capture is therefore opportunistic — that machine wrote an event file on 1 of the 8 days in the sampled window (only `3a6c7dc9-2026-08-25.jsonl` falls inside 2026-08-18 → 2026-08-25) — which multiplies every capture defect Groups 29 and 30 address. Deferred because the fix is fleet configuration outside this repo, not mm code; recorded so the product's own integration story is on the record as unverified on the maintainer's own machines. _Source: [manual] retro-fleet diagnosis 2026-08-25._

- **Decide whether the existing Grok enable verb should support usage-only operation** — Deferred product decision: `enable_source` still authorizes the usage reader and materializes the Grok source even when customization directories are absent. That coupling is not fixed. Start by assessing usage-only behavior through the existing command for the current fleet, with clear consent copy and preserved explicit user-configured customization sources. Do not precommit to `--no-usage`, `enable-usage`, and `disable-usage` as three new surfaces. Historical usage-only opt-ins are supported config state, not proof of an independent CLI switch. _Source: Track 31A /autoplan DX-3, 2026-08-27; narrowed against cli.py:enable_source and the source-toggle tests at f10bf34, user-approved 2026-09-05._
- **Rich markup eats TOML brackets in command help.** Verified live: `mm enable-source --help` renders `[sync].disabled_sources` as `.disabled_sources` and `[[sync.sources]]` as `[]`; `mm --help` renders migrate-config's `exclude_patterns` target as `[]`. Rich parses `[sync.sources]` as a style tag. The one help text carrying the Grok consent disclosure is affected. Fix by escaping the brackets or setting `rich_markup_mode="markdown"`, then audit every docstring containing `[`. Deferred: a global `rich_markup_mode` change touches every command's help and wants its own verification pass. _Source: Track 31A /autoplan Phase 3.5 DX-12, 2026-08-27._
- **`mm diag` should report Codex day / model / multi-day counts.** Would make the per-turn migration verifiable without a push: distinct days, distinct models, and the count of rollouts spanning more than one UTC day (8 of 746 measured 2026-08-28). Cut from Track 32A when `cli.py` was not yet in its `_touches`; 32A later gained `cli.py` for the diag block, so this is now cheap. Deferred as a verification nicety, not a correctness need. _Source: Track 32A /autoplan Phase 1 E-8, 2026-08-28._
- **`warm_host_cache_inline`'s docstring cites a stale measurement.** It says "573ms measured across 452 rollouts". Measured 2026-08-28 on the same machine: **801 ms across 746 rollouts, 694.3 MB, 1.15 ms/MB**. `DEFAULT_READ_BUDGET_S = 5.0` still covers it comfortably, so this is documentation drift rather than a live defect — but the number is the stated justification for the budget and it has moved 40%. Refresh whenever that function is next touched. _Source: Track 32A /autoplan Phase 3.5, 2026-08-28._

- **`~/.claude/projects` growth makes discovery linear in candidate count** — filed twice and merged here. One half named the budget symptom (at ~600 candidates it exceeds the 50 ms budget, which would pin the cursor at the floor permanently); the other named the cause: `_probe_claude` (`events.py:711`) is ~95% of post-fix discovery cost and scales with dead directories — 88 dirs on the review machine, 30 with no jsonl, 50 of 60 candidates permanently dead. mm does not own the directory, so a reaper is refused; a negative-result cache is the v0.12.15-shaped answer if it ever binds. _Source: [29A] Phase 3 Eng H4 + [30A], 2026-08-25/26._
- **`WALK_TIME_BUDGET_AUTOPUSH_MS` is the next constant to outgrow its measurement** — still 250 at `events.py:83`. At ~60 roots the per-repo timeout hits its 200 ms floor and most repos abort on every push. Track 30A's E1 makes that loud rather than silent, so the signal now exists before the fix does. _Source: [30A], 2026-08-26._
- **N6 / subdirectory recovery** — a session started in `repo/src` drops `repo`. `_classify_git_root` (`events.py:809`, re-verified at `727f9cd`) replaced `_is_git_toplevel`, which used to compute the git toplevel and then discard it. Measured 0 of 60 candidates at review time, so latent. The cheap fix is foreclosed: recovery now needs a bounded parent walk with a ceiling. Was routed to Group 30, which shipped without it. _Source: [29A] /autoplan Phase 3 Eng, 2026-08-25._
- **`mm diag`'s Rich renderer reflows long paths mid-path**, so its "optimized for support-chat paste" claim (the `cli.py` diag text path) is already false for exactly the strings a support conversation needs verbatim. _Source: [29A] /autoplan Phase 3.5 DX, 2026-08-25._
- **Grok publishes its own billed cost and mm discards it** — `params.update.usage.costUsdTicks` is on every complete Grok terminal record, and is exactly the field dropped when `usageIsIncomplete: true` (the Track 34A correlation). An authoritative per-turn cost from the host beats any rate table mm can hand-maintain, and it sidesteps the 200K-context threshold, which is not reconstructible from mm's counters. **Needs its own census first:** two sampled records imply 2.73e9 and 3.40e9 ticks per USD under published rates, so the unit does not reconcile and must not be trusted yet. Codex publishes no equivalent. _Source: [35A] /autoplan 0C-bis approach C, 2026-09-01._
- **Stable note codes for all 19 `## Notes` strings**, so the `retro_fleet/SKILL.md` decoder branches on codes rather than prose. Track-sized; Track 34A shipped a general fallback rule instead, which is why this is deferred rather than blocking. Explicitly **not** a forbidden-phrase test — this repo rejected that shape once already, on the grounds that it pins wording rather than semantics. _Source: [34A] /autoplan DX, Codex #1 reduced, 2026-08-30._

- **Intra-file resume for a Grok ledger that exceeds one read budget** — Deferred: The reader still lacks an intra-file checkpoint; the filed >1 GB / three repeated interactive deadline trigger has not been demonstrated. _Source: [plan-eng-review], inbox drained 2026-09-05; dated filing context follows._ **Why:** `_read_grok_file` has no intra-file partial stage. A `deadline` mid-file raises and discards `last_offset`, so a single ledger larger than the remaining budget can never be read. Track 46A's card named this as `_validated_grok_entry` `offset == size`; that validator never rejected (0 of 42 live entries). The real hazard is the missing mid-file checkpoint. **Repro:** largest live ledger 18.1 MB of 261 MB total / 983 ms full scan → ~265 MB/s. Wall is ~1.3 GB in one file against `DEFAULT_READ_BUDGET_S = 5.0`. **Trigger:** any single `updates.jsonl` > 1 GB, or a `deadline` that recurs across 3+ interactive warms. **Effort:** M **Priority:** P3 **Context:** filed by Track 46A /autoplan, 2026-09-04. Claude eng voice #3.

- **Host cache encoding trigger (deferred from original Track 46A)** — Deferred: Keep the measured >100 ms or >25 MB gate. The dated filing measured 23.3 ms and 4.11 MB, below both gates. _Source: [plan-eng-review], inbox drained 2026-09-05; dated filing context follows._ **Why:** `locked_json_rmw` parses and re-serialises the whole host cache every push, so cost scales with corpus size. The original 46A card's own gate does not fire. **Trigger:** json round-trip above 100 ms, or cache above 25 MB. **Repro:** measured 2026-09-04 on device 889e42c0 after a converged push: Codex `host-tokens.json` 4.11 MB / 23.3 ms / 20,047 states / 716 rollouts. Linear in states, so 25 MB ≈ 122k states ≈ 4,350 rollouts at the current mean (~6x headroom). Codex prunes its own sessions, so the corpus is not monotonic. **Effort:** S **Priority:** P3 **Context:** filed by Track 46A /autoplan, 2026-09-04. Original card premises falsified by live probe. The `host_usage._CacheEntry` docstring still points at `docs/TODOS.md` and names a "Group 46 deferral"; both are stale now that the inbox is drained and Group 46 is shipped history. Correcting that docstring is covered by the "Remaining historical Track and version comments" entry below — do it alongside the next change to `host_usage.py`, not as a standalone PR.

- **Per-entry `states` cap on the Codex host cache** — Deferred: No demonstrated pathological entry; preserve totals and cross-file dedup semantics before deciding a cap. _Source: [plan-eng-review], inbox drained 2026-09-05; dated filing context follows._ **Why:** a per-entry cap needs a degradation that keeps the file's tokens counted while dropping only its cross-file dedup. Refusing the file would re-create the fail-closed whole-store pathology Track 31A removed. `iter_bounded_lines` bounds line SIZE, not line COUNT. Max observed 2026-09-04 is 441 states/entry (card said 1,234, which was the 2026-08-28 corpus). **Effort:** S **Priority:** P3 **Context:** filed by Track 46A /autoplan, 2026-09-04. This is its own item. Do NOT fold into the former Track 49A leaf-helper task (2026-09-03 numbering), now the Future entry "Bound host-cache model detail without dropping day totals": that work bounds `_add_usage`'s `by_model` materialization and says capping in the READER is not the fix. Different symbol, opposite conclusion. The live Track 49A is unrelated snapshot-integrity work.

- **Ask whether Grok/Codex expose a supported usage surface** — Deferred: Strategic vendor-surface research has no dependency on the immediate correctness repairs. _Source: [plan-eng-review], inbox drained 2026-09-05; dated filing context follows._ **Why:** mm exact-match-parses undocumented, weekly-shipping private formats of tools it does not control, fail-closed, with no version negotiation. Both 46A review voices flagged that no approach questioned private-file parsing. Strategic, out of scope for a 3-day outage fix. **Effort:** M **Priority:** P3 **Context:** filed by Track 46A /autoplan, 2026-09-04.

- **Reader-agnostic quarantine and drift classification (Track 46B)** — Deferred: Retain the explicit next-drift / unsupported-after-upgrade trigger. The Codex diagnostic defect is placed separately and does not require quarantine. _Source: [plan-eng-review], inbox drained 2026-09-05; dated filing context follows._ **Why:** 46A tolerated exactly one additive key (`elapsed_ms`) so a 3-day outage could end. The shape that caused it is unchanged: ~13 exact-match detectors, any of which turns one additive upstream field into a total silent store abort. `_GROK_STOPS` is a closed 2-value enum and is the likely next one. The durable fix is per-record quarantine plus a `drift_skipped` counter, so a drifted record costs one record instead of the whole agent. **Scope:** per-record quarantine; `drift_skipped`; truthful coverage on day-disjoint drift (the C1 snapshot keep-set fix); a parser epoch so a later allowlist recovers tokens from a byte-identical ledger without a cache bump; widen `partial_sources` (drift is not "the host declared"). Reader-agnostic — Codex has the same detector shape. **Trigger:** the next additive-key drift on either host, or any `unsupported` that survives one `pipx upgrade`. **Effort:** L **Priority:** P2 **Context:** filed by Track 46A /ship, 2026-09-04 (plan item N5, missed by the 46A TODO sweep). Until this is carded, 46B exists only as prose in `_classify_grok_update`'s docstring, `tests/fixtures/host_sessions/grok/CONTRACT.md`, and `docs/invariants/events-retro.md:229`.

- **Remaining historical Track and version comments** — Deferred: Correct remaining historical comments alongside the files being changed; do not create a standalone code PR or carry the old count forward. _Source: [ship:severity=minor], inbox drained 2026-09-05; dated filing context follows._ **Scope:** comment/docstring-only cleanup, routed per file into a Track that already touches it. Re-grep `Track 37A|Track 37B|Track 39A|Track 42A` in `src` and `tests`; the former count of 15 is not a current measurement. **Also re-grep `Track 46B|Track 48A|Track 49A|Track 50A|Group 46|Group 50` across `src`, `tests` AND `docs/invariants/`** — the 2026-09-05 regen reused 48A/49A/50A for unrelated work, so every pre-2026-09-05 citation of those IDs now points somewhere wrong (known: `host_usage.py:1099-1100`, the `_CacheEntry` docstring). Cite unshipped work by TITLE, not by regenerated ID. **Verified at 2024ff6:** stale source-retirement references remain in `cli.py:_explicit_opencode_source_present` and in `tests/test_pull_helpers.py`, `tests/test_source_toggle.py`, `tests/test_integration.py`, and `tests/test_docs_routing.py`. The last also incorrectly says the mm-owned OpenCode link is left on disk: v0.13.0 reaps mm-owned links; the README loop covers user-made links and machines that never run an interactive command. **Partial discharge:** v0.14.1 corrected the token-pricing prerequisite commentary, and v0.14.0 corrected config's marker-skip commentary. The remaining `Track 42A` walker promises in `host_usage.py` and `tests/test_host_usage.py` ride the Codex-diagnostics Track's declared cleanup (Track 52A in the 2026-09-05 plan, Track 54A from 2026-09-06). The correct shipped Track 37A references in the bin/check tests and docs-routing verification tests stay. **Historical version check:** the original filing also named a cli.py source-retirement comment saying v0.12.54 rather than v0.12.55. Re-grep the text when that file is touched; never replace an unrelated correct bin/check version. **Effort / priority:** S / P3. Original source: [ship:severity=minor], 2026-09-03. Use dated lineage for old IDs, not blind renumbering.

- **Expose unresolved conflicts in machine-readable diag** — Deferred: Status coverage shipped; defer the residual machine-readable diag conflict inventory until a consumer needs it. _Source: [plan-eng-review:severity=major], inbox drained 2026-09-05; dated filing context follows._ **Remaining work:** `_collect_diag_state` still has no conflict inventory. Add it only with an explicit consumer and define cache-only/read-only behavior; do not run migration as a diagnostic side effect. **Partial discharge:** `status` now counts unresolved conflicts (f2624fb, v0.14.0). The original 2026-09-03 reproduction planted three sidecars and saw no status warning; that claim is no longer true. Do not rebuild the status feature. **Context:** the filing also found that non-Claude sources have no per-project sync-log breadcrumb; that remains a separate layout item. Immediate data preservation is covered by Track 48A; discoverability already has `mm conflicts` and status. **Effort / priority:** S / P2. Source: [plan-eng-review:severity=major], Track 45A /autoplan, 2026-09-03.

- **`pullhistory.append_entry` has a `sidecar=` parameter that no caller ever passes** — Deferred: Forensic filename plumbing remains useful but spans the apply outcome and pull accumulator; it is not needed to prevent the reproduced deletions. _Source: [plan-eng-review:severity=major], inbox drained 2026-09-05; dated filing context follows._ **Description:** `pullhistory.py:17` documents `"sidecar": "<optional sidecar filename if action=conflicted>"`, `:95` accepts it, `:116-117` writes it when non-None. `grep -rn "sidecar=" src/ tests/` returns zero. So every `conflicted` row records THAT a sidecar was written and never WHICH file, which makes any later reconciliation guess. This is the standing "a write with no reader is a liability" constraint inverted: a parameter with no producer, in the log schema since v0.6. **Effort:** M **Priority:** P3 **Context:** filed by /autoplan on Track 45A, 2026-09-03. NOT a 15-line plumb: `_apply_conflict` returns a bare `ApplyOutcome` Literal, `_download_and_apply` gates Track 12A's `_CANONICAL_WRITE_OUTCOMES` invalidation on that exact value, and the row is written two frames up in `_pull_core` from `outcomes[action]: list[str]` (rel_paths only). Also note `pull-history.jsonl` rotates DESTRUCTIVELY at 1 MB (`os.replace` onto `.1`, overwriting the prior `.1`, no gc reaper) and swallows all write failures by design — so it cannot host a conflict lifecycle state machine. If durable conflict state is ever needed, route it through `lockedjson.py` per CLAUDE.md, and name the field `conflict_copy=` (`sidecar.py` already means the manifest cache in this call graph).

- **Conflict GC retention age should be configurable** — Deferred: Live and recoverable copies are now preserved regardless of age; configurable retention of redundant copies is lower priority. _Source: [plan-eng-review:severity=minor], inbox drained 2026-09-05; dated filing context follows._ **Remaining work:** `retention.CONFLICT_AGE_DAYS = 30` is still fixed. An optional `[retention] conflict_age_days` setting or `mm gc --conflicts --older-than` flag would let users keep redundant copies for longer. **Safety already shipped:** since v0.14.0, live, missing-canonical and v0 copies are preserved regardless of age. This setting would affect only copies already safe to reap; the old claim that unresolved work expires after 30 days is obsolete. **Effort / priority:** S / P3. Source: [plan-eng-review:severity=minor], Track 45A /autoplan, 2026-09-03; originally deferred because it adds configuration schema.

- **Generalize per-project sync-log layout beyond Claude sources** — Deferred: Generalizing project-log layout is separate from the preservation fixes; status already exposes unresolved conflicts. _Source: [plan-eng-review:severity=minor], inbox drained 2026-09-05; dated filing context follows._ **Remaining work:** `_pull_one_source` sets `claude_sync_base` only for `src_type == "claude"`; `synclog.write_sync_log` assumes a `projects/<name>/` layout. Generic gstack/Codex sources therefore have no equivalent per-project breadcrumb. **Historical evidence:** the 2026-09-03 Track 45A review counted 22 of 25 conflicts on 2026-09-01 outside the Claude log path. This is the filing's measurement, not a fresh fleet census. **Boundary:** layout generalization is its own subsystem change. Grok Build's current mm surface is its usage reader; this does not authorize adding a Grok sync source. Status already exposes unresolved conflicts after v0.14.0. **Effort / priority:** M / P3. Source: [plan-eng-review:severity=minor], Track 45A /autoplan, 2026-09-03.

- **Share host-reader filesystem resume primitives only after measuring duplication cost** — Deferred from the former Track 49A (2026-09-03 numbering). Keep the candidate seam limited to file identity, head/tail probes and complete-line byte offsets; `iter_bounded_lines` is already shared. Prove identical full-versus-incremental output for each format before extraction, and name a concrete recurring bug or measured saving as the trigger. Codex `_TurnState` unions cumulative observations across rollouts and uses `last_token_usage`; Grok caches per-prompt terminal totals, while Claude deduplicates message IDs. A universal `(dedup_key, day, model, usage)` adapter erases information those consumers need. Shared day parsing/counter coercion may join a measured extraction, preserving peer-versus-local trust boundaries; no new AST guard for an abstraction not yet justified. The naive adapter recipe was killed, not declared shipped. _Source: former Track 49A, verified against host_usage.py and token_usage.py at 2024ff6; 2026-09-05 /roadmap. Effort M, P3._

- **Bound host-cache model detail without dropping day totals** — Deferred from the former Track 49A leaf-helper task (2026-09-03 numbering). `_add_usage` materializes model detail in `by_day[day]["by_model"]`; `events._cap_by_model` applies wire caps later. First measure actual model cardinality and memory/cache cost, then cap only expendable detail with an honest residual (`day_total - sum(by_model)`). Rejecting model records in the reader or capping the file's accounting loses totals and is not this fix. Keep it separate from the Codex per-entry `states` cap, which bounds cumulative dedup history rather than model detail. _Source: former Track 49A, symbols rechecked at 2024ff6; 2026-09-05 /roadmap. Effort M, P3._
- **Decide whether unchanged pulls should retry stale conflict-copy cleanup** — Deferred: a policy choice, not a defect. After v0.14.2 a replacement that publishes but fails to unlink the older sidecar leaves both revisions; an identical later pull takes the no-mutation branch and GC preserves the older copy while it diverges from canonical. `mm resolve` can inspect both. Adding deletion to a formerly unchanged path could discard edits made directly in a managed sidecar, so decide scope and user expectation first. Pinned by `tests/test_conflict_copy.py::TestConflictPublishThenCleanup::test_cleanup_failure_then_unchanged_retry_leaves_extras`. Hypothesis (untested): reuse the exact-owner/era/peer predicate only after verifying a matching current sidecar exists; preserve every unverifiable candidate; compare with keeping the manual resolve path. Do not call differing revisions duplicates or infer safe deletion from sidecar mtime (the peer clock). _Source: [plan-eng-review:severity=minor], Track 48A /autoplan, 2026-09-05; inbox drained 2026-09-06. Effort S, P3._
- **Historical blob integrity inventory and verified targeted re-upload** — Deferred: the detector already shipped. v0.14.3 rejects mismatched incoming plaintext per file and continues, so a historically poisoned key (two files sharing a digest, one edited between hash and upload, before `0c1a969`) now surfaces as a repeated `content check failed:` line — but only from interactive `mm pull`: `cli.py:2539` gates that line on `not quiet`, and autopull passes `quiet=True`, so autopull shows only a generic failed-file count with no per-file cause (`test_quiet_mismatch_has_no_per_file_stdout` pins the suppression) — and only for a file that Mac actually needs. The 49A reproduction proves the mechanism, not that any live fleet blob is corrupt, and no fleet telemetry establishes whether it has fired anywhere (**correction, 2026-09-06 /ship adversarial review** — the prior text asserted "has not fired" as established fact; that is unverifiable from repo state alone). **Trigger:** that line observed via interactive `mm pull` on any Mac, or an autopull failed-file count worth re-checking with an interactive pull to see the specific cause. **Then:** a bounded read-only inventory keyed by device/source/path/digest with valid / unavailable / mismatched states, reusing `_do_gc`'s all-device referenced-key walk (it already holds the passphrase), followed by a separate repair that accepts only local bytes hashing to the advertised key, encrypts before storage, verifies the result, never auto-selects a peer copy and never runs fleet-wide mutation as discovery. Interim workaround: editing the affected file on the publisher changes its digest and uploads under a fresh key. Distinct from the host-snapshot "Snapshot-level completeness" data-model entry. _Source: [plan-eng-review:severity=major], Track 49A /autoplan, 2026-09-06 (plan `~/.gstack/projects/kbitz-mind-meld/kbitz-49a-complete-snapshots-plan.md`, evidence `49a-reproductions.json`); inbox drained 2026-09-06. Effort M, P2._
- **Convert the remaining plain-stderr `safe_str` sites in events_tail.py and skill_link.py to `safe_terminal_str`** — Deferred: Track 52A's second bullet (2026-09-06 plan) intentionally narrows to 3 of 12 identified plain-stderr `safe_str` call sites (retention.py x1, token_usage.py x2) to avoid a `_touches:` collision with Track 55A, which already touches `events_tail.py` and would force a repack if this card grew onto the same file. The other 9 are all plain `sys.stderr.write(f"mm: notice: ...")` calls with no Rich markup — the identical shape Track 52A's bullet 2 fixes: `events_tail.py` (8 sites — `:277`, `:382`, `:540`, `:986`, `:1045`, `:1082`, `:1106`, `:1187`) and `skill_link.py` (1 site — `:1079`). **Trigger:** Track 55A ships (clearing the `events_tail.py` touches collision) or a maintainer decides via `/roadmap` that repacking now is worth it. **Effort:** S **Priority:** P3 **Source:** [ship:adversarial-review], /ship on branch `kbitz/roadmap-update-v2`, 2026-09-06 — finding independently surfaced by both the Claude adversarial subagent and the Codex adversarial pass, confirmed by direct read of all 9 call sites.
