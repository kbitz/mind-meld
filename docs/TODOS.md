# TODOS

Inbox for unprocessed items. Other skills (`/full-review`, `/investigate`,
`/pair-review`, manual) append here; `/roadmap` drains and organizes into
`docs/ROADMAP.md`.

## Unprocessed

- [codex-review 2026-05-12] **Defer inline `keep-canonical` mtime bump to end-of-pull-batch** (v0.12.6 follow-up). `mm pull --conflict-mode prompt` choosing (l)ocal currently returns `"skipped"` without bumping canonical's mtime — symmetric with deferred `mm resolve` would loop, BUT bumping mid-walk caused a worse regression where later peers iterating the same file silently mtime-skip (Codex P2 4th pass on v0.12.6 PR; see CHANGELOG v0.12.6 last paragraph). **Right fix:** track inline keep-canonical decisions in a `pending_inline_bumps: dict[Path, float]` plumbed through `_apply_incoming_file` → `_download_and_apply` → `_pull_core`; at end of pull (after all peer walks complete), apply `_bump_canonical_mtime_post_resolve(path, max_peer_mtime)` once per file. Beats every peer that was walked, doesn't pollute mid-walk decisions. _src/mind_meld/cli.py: ~20 LOC plumbing + 2 tests (single-peer inline propagates, multi-peer same-file doesn't suppress later peer). Tests/test_conflict_copy.py::TestResolveLocalMtimeBump::test_inline_pull_keep_local_does_not_mutate_canonical_mtime currently pins the intentional gap — flip its assertion once batching lands._
- [eng-review] **gc_cache_entries: validate max_age_s=0 / fractional-day semantics** — `int(max_age_s / 86400)` silently floors fractional days; `max_age_s=0` effectively reaps every entry. Pre-existing behavior, never tested. Add explicit tests for `max_age_s=0` (reap-all) and `max_age_s=43200` (12 hours; floors to 0 days = reap-all? or document as not-recommended). Either pin current behavior with tests or guard the input. _src/mind_meld/token_usage.py:938-947, ~10 LOC test._ Surfaced by Codex outside-voice on Track 11A review (2026-05-10). Depends on: Track 11A merging.
- [eng-review] **Add direct cache-isolation positive test** — autouse `_isolate_token_cache` fixture relies on monkeypatch at runtime, but there's no positive test asserting no real `~/.config/mind-meld/session-tokens.json` was touched during a pytest run. Stat-snapshot the real path before suite start; assert mtime unchanged after suite end (or use a `HOME`/`XDG_CONFIG_HOME` trap). Stronger regression pin than the chosen "pytest twice output identical" pre-flight. _tests/conftest.py + new tests/test_isolation_guard.py, ~15 LOC._ Surfaced by Codex outside-voice on Track 11A review (2026-05-10). Depends on: Track 11A merging.
- [plan-eng-review 2026-05-10] Skills-walk-complete signal as explicit schema field (deferred from Track 11B Option B). **What:** Add `skills_walk_complete: bool` field on the v=2 sessions-snapshot project metadata (additive, total=False, no schema bump). Aggregator picks latest snapshot per `(device, source_root, claude_dir)` where `skills_walk_complete=True` for skills aggregation; falls back to flagging `skills_incomplete_peers` when no completed walk exists in window. **Why:** v0.12.4 Option C (cosmetic-only) admits ambiguity in the "Skills incomplete" breadcrumb — the wire can't tell pre-v0.11.27 peers apart from v0.11.27+ peers whose skill walk was skipped this push. If that disambiguation becomes operationally valuable (e.g., supporting older mm in the fleet long-term, or cleanly differentiating "upgrade your peer" from "run mm push interactively"), this is the right architectural fix. **Pros:** Three-state discriminator (pre-v0.11.27 / complete / skipped); preserves last-populated-skills per device-project; no data loss via latest-snapshot-wins. Symmetric extension is possible for tokens too. **Cons:** ~20 LOC source + ~30 LOC tests. New schema field surface. Engineering for a value (pre-v0.11.27 disambiguation) that decays as fleet upgrades. **Context:** Codex outside-voice review on Track 11B caught the data-loss-via-overwrite footgun in the original 3-LOC events.py fix. Option C ships now (v0.12.4); Option B is the bigger-hammer alternative if Option C's ambiguity proves operationally annoying. **Depends on / blocked by:** Nothing today. Re-evaluate value when fleet upgrade pressure on pre-v0.11.27 ages out (likely within weeks of Track 11B landing). **Do NOT reintroduce the rejected 3-LOC fix in `events.py:_scan_one_project` (drop `if token_cache_files is not None:`)** — it causes latest-snapshot-wins data erasure on warm-then-cold push ordering. See CHANGELOG v0.12.4 and `docs/invariants/events-retro.md` "Why not always set" section.
