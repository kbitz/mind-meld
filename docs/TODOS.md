# TODOS

Inbox for unprocessed items. Other skills (`/full-review`, `/investigate`,
`/pair-review`, manual) append here; `/roadmap` drains and organizes into
`docs/ROADMAP.md`.

## Unprocessed

- **[review]** `sidecar.write` should fsync file contents + containing directory before/after `os.replace`, to make the sidecar durable against crash/power-loss between write and disk flush. Matches the pattern that `storage/local.py:put` should also adopt. **Why:** a sidecar-write-survives-rename-but-not-content scenario silently drops the sidecar on next recovery (shape check fails → peer fallback). **Size:** S. **Touches:** `src/mind_meld/sidecar.py`, possibly `src/mind_meld/storage/local.py`.
- **[review]** `_recover_prior_manifest`'s refuse message says "Run 'mm init' if storage is unrecoverable" — but `mm init` doesn't delete the corrupt manifest, it generates a new device_id. Rewrite the guidance to say "delete `manifests/<device_id>/manifest.json.enc` from storage" or add a dedicated `mm recover --reset-manifest` subcommand. **Why:** current message leads users to a resolution path that silently zeroes their deletion record. **Size:** S (message) or M (new subcommand). **Touches:** `src/mind_meld/cli.py`.
- **[review]** `_error()` writes via `console.print` which goes to stdout. In autopush/autopull quiet mode, this violates the "silent, one-line output" contract documented for `mm autopull`/`mm autopush` in README.md. Route `_error` output to stderr. **Why:** autopush failures currently emit both a rich stdout line AND the outer plain-text stderr line, confusing Claude Code integration. **Size:** S. **Touches:** `src/mind_meld/cli.py`.
- **[review]** `list_devices` silently drops corrupt `devices/*.json` entries (`devices.py:53`). If a peer's devices.json is corrupt but their manifest is intact, peer fallback misses that peer. Consider (a) logging a warning, (b) using blob directory listing as a secondary peer-discovery path. **Why:** device-metadata corruption becomes a single point of failure in the corruption recovery chain. **Size:** M. **Touches:** `src/mind_meld/devices.py`, `src/mind_meld/cli.py:_collect_peer_tombstones`.
- **[review]** `_merge_manifests` timestamp tiebreak is non-deterministic across devices when two conflict copies have identical ISO timestamps (same-second double-write). Add `(timestamp, device_id, content-hash)` sort key. **Why:** cross-device reproducibility of a merged manifest is needed for debugging "why did device X produce different tombstones than Y?". **Size:** S. **Touches:** `src/mind_meld/cli.py:_merge_manifests`.
- **[plan-eng-review]** Audit cli.py `Optional[X]` returns for conflating "not-found" vs "error". Codex flagged during Track 1A review (2026-04-22) that `_fetch_remote_manifest` was the visible symptom of a wider pattern. After the tri-state migration lands, sweep other `-> X | None` / `-> Optional[X]` signatures in cli.py and classify: (a) None is genuinely a single meaning, (b) None conflates ≥2 states and callers would benefit from an explicit result type, (c) borderline — document the meaning of None in the docstring. **Why:** prevents the same class of bug re-emerging in `diff`, `status`, `gc`, `resolve`, etc. **Pros:** prevents silent recovery failures. **Cons:** risk of rabbit-hole; bound it by time, not by completeness. **Context:** emerged from /plan-eng-review outside-voice on 2026-04-22 for Group 1 Track 1A. **Blocked by:** Track 1A Task 1 landing first (establishes the tri-state convention). **Size:** S-M. **Touches:** src/mind_meld/cli.py.
