# TODOS

Inbox for unprocessed items. Other skills (`/full-review`, `/investigate`,
`/pair-review`, manual) append here; `/roadmap` drains and organizes into
`docs/ROADMAP.md`.

## Unprocessed

- **[review] `_save_and_register` needs register-failure rollback** — `cli.py:_save_and_register` persists config → registers device → stores passphrase. If `register_device` raises (iCloud hiccup, transient StorageError) after `save_config` succeeded, the local config now claims a `device_id` that `devices/` on storage doesn't contain. Peers scanning `devices/` via `_select_devices` never discover this device — push writes manifests under an ID no one is listening for. Pre-existing behavior (present before Track 2A; Codex adversarial review 2026-04-24 flagged during /review). Fix options: (a) swap order to register BEFORE save_config, (b) on register failure, delete the saved config, (c) add a lazy self-repair path in push that calls register_device if the device file is missing. Option (a) is simplest but config-without-device is its own ugly state; option (c) is most robust. _src/mind_meld/cli.py, src/mind_meld/devices.py, ~40 lines._ (S)
- **[review] Cross-device source rename drift partitions sync** — Track 2A's type-keyed sync-log fix addresses SAME-device renames. Cross-device, manifests are still keyed by `src_name` (`manifest.py`, `_pull_core`'s `local_sources_map[src_name]` lookup), so if device A renames "claude" → "work-claude" but device B keeps "claude", B's pull skips A's manifest via the unknown-source warning path. Codex adversarial review 2026-04-24. Fix: cross-device source identity needs to key off `(type, signature)` or similar, not raw name. Bigger design change — likely a follow-up track or a SPEC.md-documented known limitation for v1.0. _src/mind_meld/cli.py, src/mind_meld/manifest.py, SPEC.md, ~100 lines._ (M-L)
- **[manual] `mm resolve` prompt labels are misleading — "(f)orce conflict → canonical" buries the user-facing meaning** — At cli.py:3629-3633 the interactive prompt reads `(c)anonical / (f)orce conflict -> canonical / (b)oth [default] / (a)bort`. The user has no way to know that "canonical" currently holds the *remote* bytes and "conflict" holds the *local* bytes, so picking (c) silently throws away the local edit and (f) is the action that restores the user's working file. User hit this 2026-04-24 when reverting 6 unwanted swaps — had to be told which key to press because the label "force conflict → canonical" doesn't read as "keep my local version." The same internal-jargon problem also lives in (a) the `mm conflicts` table column labels (Conflict/Canonical) — captured in the table-truncation TODO — and (b) the post-pull summary line ("Run `mm conflicts` to review, `mm resolve` to pick a winner") which never names what's at stake. Fix this one: relabel the resolve prompt to user terms — `(l)ocal / (r)emote / (b)oth [default] / (a)bort` — with a one-line preface above the diff that names which side is which (`Keep your local edit, take the remote version, or both?`). Letter keys must remain unique among the four; `l/r/b/a` works. Pair with the "invert conflict default" TODO so that the labels match the file-on-disk reality after the flip (canonical = local, sidecar = remote). Lower-risk to land independently if the inversion takes longer — this fix only relabels the prompt; the underlying behavior of each branch stays the same. Surface: cli.py:3629 prompt copy + cli.py:3633 letter parsing + the `(p)romote / (d)elete / (s)kip` branch at cli.py:3568-3576 (canonical-missing case) which has the same jargon problem; tests for choice parsing. _src/mind_meld/cli.py `_resolve_interactive_loop`, ~20 lines._ (XS)
- **[manual] Pull summary doesn't name conflicted files inline — counts only** — `_print_pull_summary` cli.py:2197-2250 prints per-source counts (`gstack: 231 written 15 merged 4 skipped 6 conflicts`) and a closing line directing the user to `mm conflicts` / `mm resolve`. The 6 conflicting paths are accumulated in `r.outcomes["conflicted"]` (per cli.py:2205) but never displayed. Cost: user has to run a second command (`mm conflicts`) just to learn *which* files are in conflict, then a third (`mm resolve`) to act. User feedback 2026-04-24: "there were conflicts, but what were they?" — exactly this gap. Fix: when `src_conflicted > 0` (and not quiet), list each conflicted path under the per-source line — same treatment for `failed`. Cap at, say, 20 to avoid screen-filling on a pathological pull (with `... and N more` if exceeded), or just print all of them since conflicts are rare and hiding them defeats the point. Apply equally to `failed` paths (currently equally invisible). Bonus: print the relative path (under the source's base) rather than the absolute path so each line is short — the source name in the header already disambiguates which root the relative path is under. Surface: cli.py:2197-2250 only; data is already in `_PerSourceResult.outcomes`. _src/mind_meld/cli.py `_print_pull_summary`, ~10-15 lines._ (XS)
- **[manual] `mm conflicts` table truncates filenames + uses jargon column labels** — Screenshot 2026-04-24: at typical terminal width the Conflict and Canonical columns both display `kbitz-cl…` / `kbitz-fi…` etc., so the user can't read which files are in conflict. Two compounding problems: (1) **wasted width** — both columns repeat the same parent path `/Users/kb/.gstack/projects/...`, leaving no room for the actual filename; (2) **column labels "Conflict" and "Canonical" are mm-internal jargon** that don't tell the user what each side *is*. Per the related "invert conflict default" TODO, after the flip the canonical column will hold *local* bytes and the conflict column will hold *remote* bytes — so the right labels are "local" and "remote." Fix surface (cli.py:3289-3317 `conflicts()`): (a) collapse `~` for paths under `$HOME` so the prefix is shorter; (b) since the two paths only differ by the `.sync-conflict-<ts>-<device>` infix, drop one column and show a single "Path" column with the canonical relative path, plus a separate "Sidecar" column with just the conflict suffix (or move the device/age info to a tooltip-style second line per row); (c) rename labels to "local" / "remote" — match the user's mental model and pair with the inversion TODO above; (d) use Rich's `Table(no_wrap=False, overflow="fold")` or `min_width`/`max_width` per column so long paths wrap rather than truncate. Lowest-risk standalone change: just (c) + (d) — relabel and stop truncating. Do (a)+(b) when reworking the layout. _src/mind_meld/cli.py `conflicts()`, ~20-40 lines._ (S)
- **[BUG] `mm autopull` / `mm autopush` no longer exit silently on a fresh (un-initialized) machine — visible-failure contract over-triggers** — `tests/test_integration.py::TestAutoCommands::test_autopull_no_config_exits_silently` and the `autopush` twin both fail on `kbitz/pull-hang-debug` (and presumably on every branch since the regression landed). The tests assert `result.output == ""` when `CONFIG_PATH` points at a non-existent file; instead they observe `mm: push failed - init: config not found at <path> — run 'mm init' first.\n` on stderr. Pre-existing — not introduced by this branch (only `docs/TODOS.md` changed). Likely cause: v0.8.1 visible-failure contract widening — `ConfigError` is now uniformly loud-on-stderr, but the "not initialized at all" case (the literal `init: config not found` message in `config.py:71`) is supposed to remain silent per CLAUDE.md's `mm autopull` / `mm autopush` contract: *"Both exit silently if mm is not initialized (no config) or no changes exist."* Both kinds of failure raise `ConfigError`, so the broad `except ConfigError` in the auto-command path can't distinguish "you typed `mm init` wrong" (loud — visible-failure contract) from "this Mac has never run `mm init` and probably shouldn't bark on every cron tick" (silent — original intent). Real-world impact: a fresh Mac (or a Mac where the user hasn't run `mm init` yet) sees stderr noise on every Claude Code session start if the user wired `mm autopull` / `mm autopush` into their session hooks — exactly the kb-mbp first-pull scenario from this conversation. Fix options: (a) preflight `CONFIG_PATH.exists()` in the autopull/autopush entrypoints BEFORE calling `load_config` and short-circuit silently — keeps `ConfigError` loud for genuine malformed-config cases; (b) split `ConfigError` into `ConfigNotFoundError` (silent in auto-commands) and `ConfigError` (loud, malformed); (c) sniff the error message — fragile, do not. Prefer (a) — minimal surface, matches how `_get_config()` already short-circuits in the interactive commands. Add a regression test that exercises both quiet-on-no-config AND loud-on-malformed-config (the existing test only covers the no-config branch). Source pointers: the autopull/autopush entrypoints in `src/mind_meld/cli.py` (search for `def autopull`/`def autopush`) and `load_config` in `src/mind_meld/config.py:67`. Discovered 2026-04-24 during /ship of the docs/TODOS.md inbox-grooming PR — flagged as pre-existing, deferred via `B) Add as P0 TODO`. _src/mind_meld/cli.py auto-command entrypoints + tests/test_integration.py::TestAutoCommands. ~15-30 lines including regression test._ (S) **Priority: P0 — silent-mode contract regression, surfaces on every fresh Mac with the Claude Code integration.**
- **[BUG] `_synced_scan_dirs` misses `include_files` sidecars — `mm conflicts` / `mm resolve` / `mm gc --conflicts` all undercount on `generic` sources** — At cli.py:3231-3237, the `generic` branch of `_synced_scan_dirs` returns only the resolved `include_dirs` paths. The trailing comment says `# generic: include_dirs (resolved) + base for single-file includes` but the `+ base for single-file includes` half is never implemented — `base_path` (or the parent of each `include_files` entry) is not added to the returned dirs. Result: any conflict file produced for a top-level `include_files` entry (gstack default = `config.yaml`, `.completeness-intro-seen`, `.telemetry-prompted`, `.proactive-prompted`, `.welcome-seen`, `.codex-desc-healed`, all sitting directly under `~/.gstack`) is invisible to every command that calls `_find_conflict_files`: `mm conflicts` (undercounts the table), `mm resolve` (can't surface the conflict for resolution), and `mm gc --conflicts` (won't reap stale sidecars after CONFLICT_AGE_DAYS — they accumulate forever). Reproduced 2026-04-24: kb-mbp first pull reported `6 conflicts`, `mm conflicts` listed 5; the missing one was `~/.gstack/config.sync-conflict-20260424-233316-889e42c0.yaml` (canonical: `~/.gstack/config.yaml`, an `include_files` entry), confirmed via `find ~/.gstack -maxdepth 1 -name '*.sync-conflict-*'`. Fix: in `_synced_scan_dirs`, after the `include_dirs` loop, walk the unique parents of every `include_files` entry but at **depth 0 only** — recursing would scan unsynced subtrees of `~/.gstack` that mm never touches (sessions, caches, etc.), reintroducing the original bug `_synced_scan_dirs` was written to prevent. Two viable shapes: (a) change the return type to `list[tuple[Path, bool]]` where the bool is `recursive`, then have `_find_conflict_files` switch between `rglob` and `glob` based on the flag; (b) add a separate `_include_files_conflict_paths(src_cfg, base_path)` helper that for each `include_files` entry computes the candidate conflict-file glob (`<parent>/<stem>{CONFLICT_INFIX}*<suffix>`) and returns matching paths directly, then merge those hits into `_find_conflict_files`'s output. (b) is more surgical and avoids touching the rglob loop. Both fixes need test coverage: a generic source with both include_dirs and include_files, conflicts in each, all three commands (`conflicts`, `resolve`, `gc --conflicts`) seeing the right count. Adjacent: while in there, double-check `mm push` doesn't have a parallel scope mismatch — if push misses sidecars under include_files paths, the bug compounds (sidecars never propagate via tombstone, but they shouldn't propagate anyway; verify push doesn't accidentally pick them up either). _src/mind_meld/cli.py `_synced_scan_dirs` + `_find_conflict_files` + tests for generic source with include_files conflicts. ~30-50 lines._ (S)
- **[manual] `mm resolve` only picks a winner — add real merge so output looks like one machine did all the work** — Today `_resolve_interactive_loop` (cli.py:3546-3658) prints a unified diff then offers (c)anonical / (f)orce conflict→canonical / (b)oth / (a)bort. No path to a *unified* file that combines edits from both sides. Auto-merge exists at pull time but `should_merge` (merge.py:41-44) only covers `.jsonl` (set-union by `ts` field) and `MEMORY.md` (line-union); every other text file (regular `.md` notes, `.json` configs, `.py` source, `.txt` scratch) becomes a conflict file with no merge path. **Goal:** end state is a single file that looks like all the work happened on one machine — not "pick one and lose the other side's edits." Three implementation directions, in increasing order of complexity/power: **(1) `git merge-file` two-way** — universally available on macOS, does a clean line-level union for text. mm doesn't currently store a common ancestor (push only records current state), so this is a two-way merge using one side as the base. Sensible default for code/configs; produces conflict markers when hunks overlap, then user resolves in `$EDITOR`. **(2) Common-ancestor tracking** — push records the version-at-push-time so the next pull can fetch it as the merge base for a real three-way merge. Eliminates conflict markers in many cases. Cost: extra blob storage, extra manifest field, GC needs to know about ancestor refs. **(3) AI-assisted merge for prose** — for `.md` / `.txt` / memory files, feed both versions to Claude API (project context: this is mind-meld syncing claude/gstack memory + notes — file types are heavily prose) and produce a unified version that reads as if one author wrote it. Best fit for the user's stated goal. Concerns: requires opt-in (sends content to Anthropic API, separate from mm's local-only crypto guarantee), needs a clear "review the merged version before accepting" gate, costs API tokens. **Pragmatic hybrid recommendation:** add a per-filetype merge dispatch in `merge.py` extending the existing `should_merge` table — code/JSON/text get `git merge-file`, prose (`.md` non-MEMORY, `.txt`) gets AI merge with explicit `--ai-merge` opt-in flag, binaries fall back to current pick-a-winner. `mm resolve` gains a (m)erge option in the prompt that runs the appropriate backend, drops user into `$EDITOR` if the merge produced conflict markers, then offers accept/reject. Surface: (a) `merge.py` — new dispatch table, `git merge-file` wrapper, optional `claude_api_merge` backend behind opt-in; (b) `_apply_conflict` cli.py:921 — try the merge backend before falling through to conflict-rename, only conflict-rename on merge failure; (c) `_resolve_interactive_loop` cli.py:3546 — add (m)erge action calling the dispatch; (d) config/CLI flag for AI merge opt-in + Anthropic API key handling (keyring or env); (e) tests for each backend; (f) SPEC.md note on the new merge model and the ancestor-tracking question; (g) CHANGELOG. **Open questions for the implementing session:** Track ancestor or stay two-way? AI merge as a separate command (`mm merge --ai`) or integrated into `mm resolve`? How to handle a merge that the user inspects and rejects — back to pick-a-winner, or back to `.sync-conflict-*` for a future try? Hard prerequisite: the "invert conflict default" TODO above should land first, since the merge backend's "local side" semantics get clearer once canonical = local. _src/mind_meld/merge.py + cli.py `_apply_conflict` and `_resolve_interactive_loop`, optional new module for AI backend, config + tests + SPEC.md. ~300-500 lines depending on whether AI merge ships in v1 of this feature._ (L)
- **[manual] Invert conflict-resolution default: keep local at canonical, route remote to `.sync-conflict-*`** — Today `_apply_conflict` (cli.py:921-964) on `--conflict-mode keep-both` (the default) renames the user's local file to `<stem>.sync-conflict-<ts>-<device>.<ext>` and writes the remote bytes to the canonical path. This is the wrong default in two ways: (1) **asymmetric blast radius** — local is known-working on this machine; remote is unknown-from-elsewhere. Replacing canonical with remote bytes can silently break behavior (a config the user just tuned, a project file Claude reads, a memory file the user edited) while the working version is buried in a sidecar that's easy to miss. The `.sync-conflict-*` file is *visible* (different name → user notices) so it should hold the *surprising* version, not the working one. (2) **mtime-skip already handles the "local newer" case** (cli.py near `_apply_incoming_file`'s [S] branch), so the conflict path only fires when remote is newer or mtimes are equal/missing — but "remote newer" ≠ "remote correct for this machine." User hit this 2026-04-24 on first pull on kb-mbp: 6 conflicts, working local files swapped for remote bytes at canonical, no inline indication of which 6 paths were affected. Proposed fix: swap the rename direction in `_apply_conflict` so canonical keeps the local bytes and `<stem>.sync-conflict-<ts>-<device>.<ext>` holds the remote bytes. Filename convention stays the same — the device-suffix already names the *source* of the sidecar bytes, which under the new semantics is the remote, matching the existing `remote_device_id` argument. Surface area: (a) `_apply_conflict` body cli.py:932-963 (write remote to conflict_path, leave local alone — rollback semantics get simpler since canonical is never touched); (b) preflight message at cli.py:736 ("would rename local to .sync-conflict-*" → "would rename remote to .sync-conflict-*"); (c) `--conflict-mode keep-both` docstring at cli.py:1777 + the help text users see; (d) `_prompt_conflict_choice` interactive labels (cli.py:1024-1042) — "keep-canonical" still means "keep what's at canonical now," but now that's local by default, so the prompt copy needs to make the choice clear ("keep local" / "use remote" / "keep both"); (e) tests covering conflict outcomes; (f) SPEC.md if it documents conflict-write direction; (g) CLAUDE.md mention of "Syncthing convention" — Syncthing's actual convention is that the *loser* gets the suffix and the active user's edits win, which is what this change moves toward, so the CLAUDE.md text gets *more* accurate. Alternative worth considering: instead of a hard flip, add a `--conflict-prefer {local,remote}` flag with default `local`. Slightly more complexity but explicit and reversible per-invocation. **Open question for the implementing session:** is this a v1.0 BREAKING change or a flag-with-new-default? Strong opinion that the flip is correct; weaker opinion on whether to expose the knob. Touches push/pull semantics so deserves its own SPEC.md note + CHANGELOG BREAKING entry either way. _src/mind_meld/cli.py `_apply_conflict` + `_prompt_conflict_choice` + preflight strings, tests, SPEC.md, CHANGELOG; ~80-150 lines including tests._ (M)
- **[manual] `mm pull` is silent during the download loop — add progress output** — Once `Pulling from <device>...` prints (cli.py:2390), the entire `_download_and_apply` loop (cli.py:1074-1112) runs with zero per-file output. `backend.get(bkey)` is a plain `path.read_bytes()` against the iCloud-synced storage folder; on first-pull-on-a-new-Mac, blobs exist as iCloud placeholders, so each read blocks while macOS materializes the file on demand. Real-world impact (kb-mbp first pull, 2026-04-24): 286 files, 263.2s wall, no output between the per-device header and the final summary — visually identical to a hung process. `--verbose` does NOT help: verbose paths fire inside `_apply_incoming_file` per-outcome and inside `_print_pull_summary`, both AFTER each file's blocking read completes. Fix options: (a) Rich `Progress` with a single updating "downloading N/total" line when stdout is a TTY (Rich is already imported at cli.py:21, just need `from rich.progress import Progress`); (b) plain "downloading N/total" counter every K files for non-TTY paths (`console.is_terminal` gate); (c) both — Progress for interactive `mm pull`, plain counter for piped/CI use, no output for `quiet=True` (autopull). Watch out for: don't print before `backend.get` returns — that's where the time actually goes; total count is `len(to_download)` per source, available before the loop; bytes-transferred is already tracked, can be displayed too. Adjacent gap (separate TODO if pursued): mm has no iCloud-placeholder awareness — could `brctl download` the storage root before the read loop, or scan for `.icloud` sidecars and surface "iCloud is still syncing N files" instead of blocking silently. _src/mind_meld/cli.py `_download_and_apply` and `_pull_one_source`, ~30 lines._ (S)
- **[manual] Add `retro-context.md` + `greptile-history.md` to gstack default `include_files`** — `/retro global` optionally reads `~/.gstack/retro-context.md` (user-authored meeting notes/decisions for retro narrative) and `~/.gstack/greptile-history.md` (greptile review history for signal-ratio metric). Both live at the gstack root, are NOT in `DEFAULT_SOURCES` today, and retro silently skips them when absent — so cross-machine retro on Mac B can't see notes the user wrote on Mac A. Fix: append both filenames to the `include_files` list in `src/mind_meld/config.py:DEFAULT_SOURCES` (the gstack entry). Low-risk additive change. Users on existing configs with explicit `sync.sources` will need to add the files manually — the defaults only kick in when `sync.sources` is absent. Consider a one-line note in the CHANGELOG + the "Syncing gstack" README block pointing existing users at the new filenames. _src/mind_meld/config.py, ~4 lines._ (XS)

## Completed

- **[plan-eng-review] Enforce ruff in CI** — added `ruff==0.15.12` to dev deps, `[tool.ruff]` + `[tool.ruff.lint]` with rule selection `E/F/W/I` (isort enforcement locks in Group 3's import hoisting; F541 covered under F group), wired into `.github/workflows/ci.yml` as a separate `lint` job running `ruff check` + `ruff format --check`. One-time fix-drift commit swept 113 violations clean (29 unused imports removed, 27 import blocks sorted, 9 unused locals deleted, 1 line trimmed, 28 files reformatted). PYI041 and UP007 deferred — E/F/W/I is the floor; broader rulesets revisit post-v1.0. **Completed:** v0.8.11 (2026-04-24)
