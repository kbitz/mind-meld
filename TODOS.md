# TODOS

## P2 — Selective sync (per-project filtering)
**What:** Allow `sync.include` / `sync.exclude` in config.toml to filter which projects are synced.
**Why:** Users with dozens of Claude projects waste bandwidth syncing all when they only use 2-3 across machines.
**Context:** Syncing everything is fine for v1. Filter matching should use glob patterns against project directory names.
**Effort:** M | **Depends on:** Phase 1 complete

## P3 — Mtime-based hash caching
**What:** Cache local manifest with mtimes at `~/.config/memsync/local-manifest.json`. Only re-hash files whose mtime changed.
**Why:** Makes repeat pushes near-instant when nothing changed. Currently re-hashes every file on every push.
**Context:** Not needed until users have thousands of session files. Full re-hash is sub-second for typical usage. Mtime can be unreliable on some filesystems — may need fallback to full hash.
**Effort:** S | **Depends on:** Phase 1 complete

## P3 — Three-way merge with stored last-synced base
**What:** Track per-file "last-synced hash" in local state. On pull, use it as the merge base to distinguish "remote changed, I didn't" (safe overwrite) from "we both changed since last sync" (real conflict).
**Why:** The current design (mtime + hash) can produce false conflicts when clocks drift or when mtime is preserved across copies. A stored base makes conflict detection deterministic.
**Pros:** Correct-by-construction conflict detection. Enables smarter auto-resolution (e.g., fast-forward when one side hasn't changed).
**Cons:** Schema change — new state file `~/.config/memsync/sync-state.json` tracking per-source, per-file last-synced hashes. More moving parts, more corruption modes.
**Context:** Conflict-copy preservation (shipped in kbitz/sync-conductor-ctx) is a cheaper approximation that catches the important cases via the Syncthing model + mtime-skip. Revisit if real-world usage shows false-positive conflicts from clock drift or copy-preserves-mtime scenarios.
**Effort:** M | **Depends on:** none

## Unprocessed

- [full-review] Conflict-copy files propagate cross-device (critical) — `manifest.py:25-41` EXCLUDED does not include `*.sync-conflict-*`, so after a conflict-copy is created locally the next push walks, hashes, encrypts, and uploads it; other devices pull it as a regular source file, defeating the Syncthing model shipped in v0.4.0. Fix: add `"*.sync-conflict-*"` to EXCLUDED plus a regression test. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Tombstones lost when remote manifest unreadable (critical) — `cli.py:815-821, 868-879` if `_fetch_remote_manifest` returns None (all copies corrupt), `generate_tombstones(local, None, device_id)` returns {} and push writes a fresh manifest with no tombstones; previously-deleted files can resurrect on cross-device pull. Fix: read tombstones from OTHER devices' manifests before accepting the empty fallback, or persist tombstones in a local sidecar. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Tombstone key format drift between carry-forward and new writes (critical) — `manifest.py:463-569` carry-forward preserves verbatim tombstone keys while new writes always use `src_name:path`; pre-v2 bare-path tombstones never match `is_tombstoned(src, path, …)`, silently un-tombstoning legacy deletions on upgrade. Fix: normalize all tombstone keys to `src_name:path` at read time in `normalize_manifest`, or have `is_tombstoned` check both forms. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `_merge_manifests` union can resurrect deletions (critical) — `cli.py:172-213` takes a union of `sources.{name}.files` across conflict-copy manifests via `dict.update`, so a file deleted in the newer manifest but present in an older conflict copy survives the merged view; tombstones only save this when they propagate cleanly. Fix: key the merge off the newest timestamp's view and only carry forward older-copy entries not explicitly omitted by newer ones. Found on branch kbitz/full-review (2026-04-22)
- [full-review] autopull/autopush swallow all exceptions (important) — `cli.py:1824-1907` bare `except Exception` catches from `_pull_core`/`_push_core` surface real bugs (KeyError on malformed manifest, new crypto errors) only as one-liner stderr with no traceback; since autopull is what Claude Code runs in the background, this hides data-integrity issues. Fix: log traceback to `~/.config/memsync/autopull.log`, differentiate `MemSyncError` (expected) from bare `Exception` (unexpected). Found on branch kbitz/full-review (2026-04-22)
- [full-review] Post-push auto-GC swallows all exceptions silently (important) — `cli.py:756-761` `except Exception: pass` masks wrong-passphrase, storage-permission, and future refactor bugs; the rest of the module uses `console.print` diagnostics. Fix: catch only `MemSyncError` and print a `[yellow]Warning:[/yellow]` line; let unknown exceptions propagate. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Pull silently skips unknown remote sources (important) — `cli.py:1046-1053` a remote manifest with a source this device hasn't configured locally is skipped with a verbose-only dim message; non-verbose users see nothing and assume full sync, silent-partition risk whenever source config drifts between devices. Fix: always print a warning; include `skipped_unknown_source` in pull summary. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `_validate_sources` not run at config-load time (important) — `config.py:103-116, 57-72` `_validate_sources` is only called from `get_sources`, not from `_validate`/`load_config`; TOML with duplicate-named or missing-field sources loads fine and fails later during sync. Fix: move `_validate_sources` into `_validate` so validation is a single eager step. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `--no-prompt` pull gives no keep-both feedback (minor) — `cli.py:940-941` script mode silently keep-boths conflicts with no terminal output; users only learn from `.memsync-log.md`. Fix: document the three resolve modes in pull docstring; consider `--fail-on-conflict` for CI-style usage. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Decompose `_pull_core` (247 lines) (important) — `cli.py:961-1208` covers device discovery, manifest prefetch, tombstone collection, per-device/per-source diff, download+apply, summary printing, and sync-log writing. Fix: split into `_select_devices`, `_prefetch_manifests`, `_pull_one_source`, `_print_pull_summary`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Decompose `init` (85 lines) (important) — `cli.py:645-730` does prompting, directory creation, passphrase setup, config writing, optional gstack discovery, a second save_config, and device registration. Fix: extract `_prompt_passphrase()`, `_maybe_add_gstack_source(config)`, `_save_and_register(config)` helpers. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Decompose `_apply_incoming_file` (114 lines) (important) — `cli.py:447-561` five-branch decision tree (written/unchanged/merged/skipped/conflicted) each doing enough work to merit its own helper. Fix: extract `_apply_write`, `_apply_merge`, `_apply_conflict` helpers and dispatch via outcome classification. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Storage key patterns scattered as string literals (important) — `cli.py:136, 222, 322, 591, 878, 1486-1490` six `f"manifests/{device_id}/manifest.json.enc"` and `f"data/{device_id}/{sha}.enc"` sites plus a path parser, with no central definition. Fix: introduce `_manifest_key(device_id)` and `_blob_key(device_id, sha)` helpers in the storage package. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Conflict/tombstone constants scattered across 3 modules (important) — `CONFLICT_INFIX`, `CONFLICT_AGE_DAYS` (cli.py:54-55), `TOMBSTONE_TTL_DAYS` (manifest.py), `FORMAT_VERSION` all describe on-disk format but live in separate modules. Fix: move to a shared `memsync/constants.py` or `memsync/conflict.py`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Duplicated file-walk logic across walkers (important) — `manifest.py:143-177 and 255-285` 30 lines of per-file "stat → exclude → size-check → hash → record mtime/size/sha" duplicated verbatim between `walk_claude_source` and `walk_generic_source`. Fix: extract `_record_file(path, base, max_file_size, on_skip, files)` helper. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `init()` duplicates `DEFAULT_SOURCES` (important) — `cli.py:663-720` hardcodes `"~/.claude"`, `52_428_800`, `65_536`, and re-inlines the full gstack source dict that already exists in `config.DEFAULT_SOURCES`. Fix: import and reuse `DEFAULT_CLAUDE_DIR`, `DEFAULT_MAX_FILE_SIZE`, `DEFAULT_ARGON2_MEMORY_KB`, and pick the gstack entry out of `DEFAULT_SOURCES`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Tombstone `deleted_at` parsing duplicated (minor) — `manifest.py:488-497, 553-563` `generate_tombstones` and `collect_tombstones` both parse `deleted_at` strings with the same fromisoformat-add-utc-compare-cutoff dance. Fix: extract `_parse_tombstone_ts(iso_str)` helper. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `merge.py` strategy dispatch duplicated (minor) — `merge.py:16-35, 64, 80` `should_merge` and `merge_file` independently test `.jsonl` and `MEMORY.md`; `merge_jsonl`/`merge_lines` share an identical join-lines serialization tail. Fix: introduce `_merge_strategy(rel_path)` dispatch and `_join_lines(lines)` helper. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `write_sync_log` claude-hardcoded in multi-source world (important) — `cli.py:1147-1158` called only when `src_name == "claude"`; gstack-only users never get `.memsync-log.md`, and literal-name coupling breaks if the source is renamed in config. Fix: key the decision off `src_cfg["type"] == "claude"`, or generalize `write_sync_log` to accept a type-specific layout callback. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `synclog.py` parameter name `claude_dir` and hardcoded `projects/` layout (minor) — `synclog.py:16, 34-36` lags the multi-source rename; everywhere else the pattern is `base_path` with type-scoped layout. Fix: rename parameter to `base_path` and scope the projects/ layout in the caller. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `init` assumes `~/.claude` exists (important) — `cli.py:696-723` writes `sync.claude_dir = "~/.claude"` unconditionally; a pure-gstack user with no `~/.claude` ends with a config where `get_sources` returns `[]` and push says "No sync sources found" with no remediation. Fix: prompt for which sources to enable with auto-detect defaults; refuse to finish init if no source path exists on disk. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Version string mismatch across 3 sources (important) — `VERSION` says 0.4.0, `pyproject.toml` says 0.3.0, `src/memsync/__init__.py:3` says 0.2.0; `init` command prints `__version__` so users see 0.2.0. Fix: pick one source of truth (prefer `importlib.metadata.version("memsync")`), derive the others, or delete the VERSION file if unused. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `_delete_files` is dead code after additive refactor (minor) — `cli.py:624-638` never called after v0.3.0 additive model; a future maintainer will re-wire delete-on-pull behavior that the spec explicitly forbids. Fix: delete the function, or guard it with an assertion and a comment explaining why it's retained. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `walk_directory` and `build_manifest` have no production callers (important) — `manifest.py:182-209` kept only for `test_manifest.py`, `test_integration.py`, `test_additive_sync.py`. Fix: delete both; update the three test files to call `walk_claude_source` / `build_manifest_v2` directly. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Dead Python 3.10 `tomllib` fallback (minor) — `config.py:14-20` `sys.version_info >= (3, 11)` / `tomli` fallback is unreachable because `pyproject.toml` requires `python>=3.11`. Fix: replace with unconditional `import tomllib` and drop `import sys`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Legacy `claude_dir` defaulting in `_apply_defaults` (minor) — `config.py:86-100` still treats `sync.claude_dir` as a first-class field, but `get_sources` only reads it in the legacy fallback branch. Fix: delete the defaulting or scope it to legacy configs only, with a docstring note. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Redundant v1 `"files"` key written in v2 manifests (minor) — `cli.py:200-202, manifest.py:340-360` `_merge_manifests` and `build_manifest_v2` both write a top-level `"files"` key no downstream reader consumes; `normalize_manifest`'s v1→v2 promotion is the only real compat shim. Fix: drop the `"files"` key in new v2 manifests. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `TOMBSTONE_TTL_DAYS` imported but never referenced (minor) — `cli.py:35` unused import. Fix: remove from the import list. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `DiffResult` not a dataclass (minor) — `manifest.py:402-425` hand-written `__init__`/`__repr__` inconsistent with `PullResult`/`PushResult` (dataclasses). Fix: convert `DiffResult` to `@dataclass`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `diff_manifests` signature is v1-shaped (minor) — `manifest.py:428-459` looks at top-level `"files"` key only; every call wraps per-source data as `{"files": src_data["files"]}`. Fix: rename to `diff_files(local_files, remote_files)` taking dicts directly. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Argon2 KDF re-derives per file (important) — `crypto.py:65-73, 76-100` per-file salt forces Argon2id (64MB/3 iters) to run once per file on push AND once per file on pull; 1000-file sync ≈ 150-300ms × 1000 = multi-minute overhead, undermining manifest-diff. Fix: reuse a single salt per push/pull (store on-device), or add an LRU cache keyed by (passphrase-hash, salt); benchmark before 1.0. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `LocalBackend.put` strands tmp files on crash (important) — `storage/local.py:31-43` uses `tempfile.mkstemp(dir=path.parent, suffix=".tmp")` with no cleanup path; crash or ctrl-C leaves stranded `tmp*.tmp` files that GC skips (not `.enc`) and they accumulate indefinitely. Fix: try/except that unlinks tmp on failure; add a startup sweep of `data/**/tmp*.tmp` on each command. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `resolve --force` rename not atomic across filesystems (minor) — `cli.py:1769-1775` uses bare `Path.rename` instead of the tmp-then-rename pattern used elsewhere; cross-fs rename raises OSError and iCloud race can leave canonical half-present. Fix: use `shutil.move` or read-write-rename via `_atomic_write`; surface rename failure as non-zero exit. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Lockfile PID race between stale-detection and re-create (minor) — `lockfile.py:28-68` two concurrent msync processes can both pass "stale detected" simultaneously; only the atomic second `O_CREAT|O_EXCL` saves us. Fix: retry once after 100ms sleep, or switch to `fcntl.flock`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `device_id[:8]` truncation collision risk in conflict filenames (minor) — `cli.py:54, 541` two machines with same 8-char prefix same-second collision can clobber a prior local edit's conflict copy. Fix: use full device_id or 16-char prefix. Found on branch kbitz/full-review (2026-04-22)
- [full-review] iCloud conflict regex too permissive (minor) — `storage/local.py:17-23, 81-108` `^(.+?)\s+(\d+)(\.[^.]+)$` matches any "name N.ext" sibling without verifying it's iCloud-generated. Fix: after matching, verify (decryptable or recent mtime) before `delete_conflict_copies` unlinks; add adversarial unit tests. Found on branch kbitz/full-review (2026-04-22)
- [full-review] GC silently ignores malformed blob paths (minor) — `cli.py:1482-1497` requires `len(parts) == 3`; blobs at unexpected depth (future schema, crashed `.tmp` artifacts) live forever. Fix: log (and optionally reap) any `data/**` file that doesn't match the expected pattern; cover with a test. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `TestPushPullRoundTrip` bypasses the CLI (important) — `tests/test_integration.py:53-163` uses `build_manifest`/`encrypt`/`storage.put` by hand rather than invoking the CLI; does not exercise `_pull_core` or `_apply_incoming_file`. Fix: migrate to `runner.invoke(app, ["push"|"pull"])` like `TestMultiSourceSync` already does; once migrated, the `build_manifest` import drops too. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `test_deletion_propagation` named opposite of its assertion (minor) — `tests/test_integration.py:103-163` name reflects pre-additive behavior; body asserts B's copy is preserved. Fix: rename to `test_deletion_not_propagated_in_additive_model`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Lazy in-function imports in tests (minor) — `tests/test_conflict_copy.py` (~11 sites) and `tests/test_integration.py` (~7 sites) re-import `memsync.cli` helpers inside test bodies despite module-level blocks. Fix: hoist to module-level imports. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Missing `backend: LocalBackend` type hints on CLI helpers (minor) — `cli.py:108, 116, 124, 241, 273, 298, 332, 360, 381, 447, 564, 624, 766, 961` accept untyped `backend`, while `devices.py:13, 30, 43` types it consistently. Fix: add `backend: LocalBackend` type hints across the CLI helpers. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `Optional[X]` vs `X | None` mixed in cli.py (minor) — 6 `Optional[X]` and 11 `X | None` uses in the same file despite `from __future__ import annotations`. Fix: standardize on `X | None` and drop `Optional` from the typing import. Found on branch kbitz/full-review (2026-04-22)
- [full-review] F-strings without placeholders (minor) — `cli.py:897, 1246, 1377, 1502, 1864` literal strings marked `f"..."` with no interpolation. Fix: drop the `f` prefix. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Pull never updates local device `last_seen` (minor) — push updates (`cli.py:882`) but pull does not; a read-only device appears stale forever. Fix: update `last_seen` on pull too, or document "last_seen means last pushed" in a comment. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Bare `except Exception` in keyring helpers (minor) — `crypto.py:97, 104, 120, 147` two re-raise as `CryptoError` (OK), two in `get_passphrase`/`store_passphrase_in_keyring` silently swallow. Fix: narrow to `keyring.errors.KeyringError` + `ImportError`. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `_pull_core` calls `list_devices` twice (minor) — `cli.py:994, 1008` once to build the `devices` list to pull from, then again into `all_devices_list` for manifest prefetching. Fix: fetch once, derive the filter. Found on branch kbitz/full-review (2026-04-22)
- [full-review] `_predict_pull_outcome` vocabulary mismatch (minor) — `cli.py:241-270` returns `"write"/"merge"/"skip"/"conflict"/"unchanged"` while `ApplyOutcome` uses past tense `"written"/...`. Fix: align vocabulary with the `ApplyOutcome` literal. Found on branch kbitz/full-review (2026-04-22)
- [full-review] Tombstone "revive" edge case leaves new peers inconsistent (minor) — `cli.py:1086-1094` a file re-created locally after tombstone never clears the tombstone until TTL; new peers see it as deleted and skip downloading. Fix: emit a "revive" marker in push manifest, or document the 30-day edge case. Found on branch kbitz/full-review (2026-04-22)
